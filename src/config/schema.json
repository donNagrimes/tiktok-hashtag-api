{
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "url": { "type": "string" },
    "region": { "type": "string", "minLength": 2, "maxLength": 2 },
    "limit": { "type": "integer", "minimum": 1 }
  },
  "anyOf": [
    { "required": ["name"] },
    { "required": ["url"] }
  ],
  "additionalProperties": false
}